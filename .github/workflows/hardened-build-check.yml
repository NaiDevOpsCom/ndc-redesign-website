# This workflow verifies the integrity and security of the build artifacts.
# It ensures that:
# 1. The application builds successfully (including TypeScript validation via the build script).
# 2. No source maps are generated (preventing source code leakage).
# 3. No console.log or debugger statements remain in the production build.

name: Hardened Build Verification

permissions:
  contents: read
  pull-requests: read

# Optimized concurrency - branch-based with global limit
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

# Global environment variables for all jobs
env:
  NODE_VERSION: "22.13.0"
  CACHE_VERSION: "v1"

on:
  push:
    branches:
      - main
      - pre-staging
  pull_request:
    branches:
      - main
      - pre-staging

jobs:
  security-scan:
    name: Security Scan
    uses: ./.github/workflows/security-validation.yml
    with:
      node-version: "22.13.0"
      fail-level: "moderate"
    secrets: inherit

  verify-hardening:
    name: Verify Hardening
    needs: security-scan
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@0c366fd6a839edf440554fa01a7085ccba70ac98 # v4.2.2

      - name: Setup Node.js
        uses: ./.github/actions/cache-vite-npm
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Install and Validate Dependencies
        run: |
          echo "📦 Installing dependencies with lockfile validation..."
          npm ci

          echo "🔒 Validating lockfile integrity..."
          npx lockfile-lint \
            --path package-lock.json \
            --type npm \
            --validate-https \
            --allowed-hosts npm \
            --validate-integrity \
            --validate-package-names

          echo "✅ Dependencies installed and validated successfully"

      - name: Build Hardened App (Typecheck + Build)
        run: npm run build -- --mode production
        env:
          GITHUB_REF_NAME: ${{ github.ref_name }}

      - name: Cache Vitest
        uses: actions/cache@d4323d4df104b026a6aa633fdb11d772146be0bf # v4.2.2
        with:
          path: client/.vitest-cache
          key: vitest-${{ runner.os }}-${{ env.NODE_VERSION }}-${{ hashFiles('package-lock.json', 'client/src/utils/__tests__/hardenedBuildCheck.test.ts') }}
          restore-keys: |
            vitest-${{ runner.os }}-${{ env.NODE_VERSION }}-

      - name: Hardened Build Unit Test (CI enforcement)
        env:
          CI_ENFORCE_HARDENING: "true"
        run: npx vitest run client/src/utils/__tests__/hardenedBuildCheck.test.ts

      - name: Verify No Source Maps
        run: |
          if find dist -name "*.map" | grep -q .; then
            echo "::error::Source maps found in hardened build! Hardening failed."
            exit 1
          else
            echo "SUCCESS: No source maps found."
          fi

      - name: Verify No Console/Debugger Statements
        run: |
          # Search for actual console.* calls (not just references like typeof console)
          # Pattern matches: console.log(, console.warn(, console.error(, console.info(, console.debug(, or debugger
          # Exclude analytics files which may legitimately use console
          FOUND_FILES=$(find dist -name "*.js" ! -name "analytics*.js" -exec grep -lE 'console\.(log|warn|error|info|debug)[[:space:]]*\(|(^|[^a-zA-Z0-9_])debugger([^a-zA-Z0-9_]|$)' {} \; 2>/dev/null || true)

          if [ -n "$FOUND_FILES" ]; then
            echo "::error::Found console or debugger statements in hardened build! Hardening failed."
            echo "$FOUND_FILES"
            exit 1
          else
            echo "SUCCESS: No console or debugger statements found."
          fi
