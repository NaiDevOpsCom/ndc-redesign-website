// Removed ambient declarations for `cmdk` to allow the package's bundled TypeScript definitions to be used.
// If you need custom types, re-create precise exported types that mirror `node_modules/cmdk`.
